<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('link.book_edit'), edit_book_path(@book) %> |
  <%= link_to t('link.books_back'), books_path %>

  <%= button_to t('button.book_destroy'), @book, method: :delete %>
</nav>
